import { TrucateCustomPipe } from './trucate-custom-pipe';

describe('TrucateCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TrucateCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
